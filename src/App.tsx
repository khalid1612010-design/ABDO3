/**
 * App.tsx
 * ------------------------------------------------------------------
 * This project's build tool (Vite) requires a React entry point,
 * but the actual website for Abdullah Sayed Elmahroky is built with
 * plain, beginner-friendly files:
 *
 *   1) index.html   -> page structure (all sections & content)
 *   2) src/style.css -> all styling (dark/gold luxury theme)
 *   3) src/script.js -> all behavior (language toggle, menu,
 *                       animations, WhatsApp contact form, etc.)
 *
 * This component intentionally renders nothing. The real content is
 * plain HTML already present in index.html and is controlled by
 * src/script.js.
 * ------------------------------------------------------------------
 */
export default function App() {
  return null;
}
